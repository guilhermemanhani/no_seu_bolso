import 'package:flutter/material.dart';

class CadastrarGastoPage extends StatelessWidget {
  const CadastrarGastoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cadastrar'),
      ),
      body: Container(),
    );
  }
}
